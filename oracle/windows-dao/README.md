# Windows DAO oracle

This directory will contain the Windows-only, declarative DAO oracle. It is an
independent test and fixture-generation tool and must never become a runtime
dependency of the Rust library or CLI.

The protocol, environment record, scenario schema, and canonical snapshot
format must be validation-driven before implementation begins.

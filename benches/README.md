# Benchmarks

Criterion harnesses, dataset manifests, checked-in baselines, and resource
measurements will live here. The required operations, dataset sizes, and
regression threshold are specified in `docs/validation/`.
